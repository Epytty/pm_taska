meta:
  environment: jvm
  toolchain:
    name: maven
    version: "17"
build:
  image:
  args: clean package
  artifacts:
    target/*.jar: /
run:
  image:
  jarName: pm-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: "8080"
