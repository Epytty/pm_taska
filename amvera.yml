meta:
  environment: jvm
  toolchain:
    name: maven
    version: "17"
build:
  image:
  args:
  artifacts:
    target/*.jar: /
run:
  image:
  jarName: main.jar
  persistenceMount: /data
  containerPort: "8080"
  servicePort: "8080"
